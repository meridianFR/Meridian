import { Placeholder } from "@/components/placeholder";

export const metadata = { title: "Écosystème" };

export default function Page() {
  return (
    <Placeholder
      sectionLabel="Vision · Écosystème"
      title="Un écosystème."
      shimmer="Une couche data."
      subtitle="Le calculateur et le journal forment les deux briques d'un seul système. Calculer avant le trade. Mesurer après."
      status="soon"
    />
  );
}
